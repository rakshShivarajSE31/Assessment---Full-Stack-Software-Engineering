import React from 'react';

//Graph route of the SPA
// This component represents the Graph route of the SPA.
// It's a placeholder for graph visualization.
const GraphComponent = () => {
  return (
    <div>
      <h2>Graph Component</h2>
    </div>
  );
}

export default GraphComponent;
